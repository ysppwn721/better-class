/* ============================================================================
 * android/build.gradle.kts — 根构建脚本
 * 极简 WebView 壳工程：不依赖 Capacitor，只需要 Android Gradle Plugin。
 * ==========================================================================*/
plugins {
    id("com.android.application") version "8.5.2" apply false
}
